The following variables should be changed to match your system configuration:

- KERNEL_MODULE_EVRMA in Makefile.common at src/lib and src/test should point
  to the evrmaDriver directory.
  
- EVRMA in Makefile.common at src/test should point to the installed evrma
  library.
